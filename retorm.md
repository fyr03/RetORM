# RetORM 当前代码框架说明

## 1. 项目目标

RetORM 旨在面向 ORM 场景构建一个差分测试框架。它的核心思路不是直接手写大量测试用例，而是：

1. 定义一套统一的查询语义表示，即 IR（Intermediate Representation，中间表示）。
2. 从同一个 IR 自动生成三条“语义等价”的执行路径。
3. 对三条路径的执行结果做语义比较。
4. 一旦结果不一致，就将其视为潜在 bug，并生成日志和复现脚本。

当前代码已经实现了这个思路的基本闭环，虽然还是一个原型系统，但主干链路已经比较完整：

- 随机生成 Schema
- 随机生成 IR
- 自动生成测试数据
- 三路执行
- 结果比较
- 记录 bug 和导出复现脚本


## 2. 当前目录结构与职责

项目当前主要由以下模块组成：

### 2.1 `ir/`

`ir/nodes.py`

定义统一查询语义的 IR 节点，包括：

- `Scan`
- `Filter`
- `Join`
- `GroupBy`
- `Having`
- `Project`
- 条件表达式：`Compare` / `And` / `Or` / `Not`
- 聚合表达式：`Aggregate`

这一层是整个框架的语义核心。三条执行路径都以 IR 作为输入。

### 2.2 `generator/`

#### `generator/schema_gen.py`

负责随机生成测试用数据库模式：

- 表数量
- 列类型
- 主键
- 外键关系
- 对应的建表 SQL / 删表顺序

#### `generator/ir_gen.py`

负责在给定 Schema 上随机生成一棵语义合法的 IR 树。当前支持的生成顺序是：

`Scan -> Join? -> Filter? -> GroupBy? -> Having? -> Project`

它还维护了一个 `GenContext`，记录当前可见列、表别名、分组字段、聚合别名等上下文信息，用于保证 IR 生成的合法性。

#### `generator/data_gen.py`

负责根据 Schema 和 IR 生成测试数据并插入数据库。当前有两种策略：

- 随机生成
- 尝试使用 Z3 生成满足条件的数据，失败时回退到随机生成

### 2.3 `translators/`

这是整个系统最关键的三条执行路径：

#### `translators/python_ref.py`

程序逻辑参考实现。它会：

- 从数据库中把原始表数据取出
- 在 Python 中手工完成 `join / filter / group / having / project`

目标是“尽量简单直观”，用于充当差分测试的参考基准。

#### `translators/sql.py`

把 IR 翻译成原生 SQL，再直接提交给 MySQL 执行。

它代表的是“路径二：IR -> SQL -> DBMS”。

#### `translators/sqlalchemy_orm.py`

把 IR 翻译成 SQLAlchemy Core 查询对象，再交给 SQLAlchemy 执行。

虽然文件名叫 `orm`，但当前实际使用的是 SQLAlchemy Core，而不是 Model + Session 风格的传统 ORM。

这条路径代表的是“路径三：IR -> ORM API / 框架层 -> DBMS”。

### 2.4 `comparator/`

`comparator/compare.py`

负责三条路径结果的规范化和比较。它要解决的问题包括：

- 列名格式不一致
- 表前缀不一致
- `Decimal / float / int / bool` 等类型差异
- `NULL / NaN` 表达差异
- 无 `ORDER BY` 时的结果顺序不稳定

这一层本质上就是当前 RetORM 的语义等价判定器。

### 2.5 `db/`

`db/connector.py`

封装数据库连接、DDL、DML、SQLAlchemy engine 等基础操作。

### 2.6 `runner.py`

整个框架的入口。它负责把所有模块串起来，完成一次完整测试：

1. 初始化数据库
2. 生成 Schema
3. 建表
4. 生成 IR
5. 生成测试数据
6. 三路执行
7. 比较结果
8. 统计通过/失败/bug
9. 导出 bug 详情和复现脚本


## 3. 当前代码已经实现了什么

从“原型系统是否可跑通”的角度看，当前代码已经具备以下功能。

### 3.1 统一 IR 驱动三条执行路径

这是当前实现最核心、也是最有价值的一点。

目前不是分别为 SQL 和 ORM 手写不同测试逻辑，而是先生成一份统一 IR，再分别交给：

- Python 参考执行器
- Raw SQL 翻译器
- SQLAlchemy 翻译器

这使得三条路径在输入层面天然对齐，符合差分测试的基本要求。

### 3.2 支持基础关系查询结构

当前 IR 已支持：

- 单表扫描
- 等值内连接
- `WHERE`
- `GROUP BY`
- 聚合函数：`SUM / COUNT / AVG / MAX / MIN`
- `HAVING`
- `SELECT` 投影

框架已经覆盖了 ORM 中最常见的一批查询结构。

### 3.3 基础随机测试能力

当前已经能够自动化地：

- 生成不同 Schema
- 在同一 Schema 下生成多条查询
- 为每条查询生成数据
- 进行批量测试


### 3.4 初步 bug 工程化支持

当前已经实现：

- 运行日志
- 详细日志
- bug 详情日志
- bug 复现脚本导出



## 4. 当前实现存在的主要问题

### 4.1 复现脚本不一定真的可复现原始 bug

在 `runner.py` 中，bug 复现脚本并不是把“原始失败时的真实数据”保存下来，而是重新调用生成器再生成一遍数据。

问题在于：

- 复现脚本里 `_gen_insert_code()` 用的是 `query_seed` 重建 `_schema`
- 原始运行时生成 Schema 用的是 `schema_seed`
- 复现脚本里还把 `rows_per_table` 固定成了 `10`
- 并且把 `use_z3` 固定成了 `False`

这会导致复现脚本和原始失败运行之间出现偏差：

- Schema 可能不同
- 数据可能不同
- 数据生成策略可能不同

最终结果就是：日志显示发现了 bug，但导出的“复现脚本”不一定能复现。

#### 修复

1. 直接把原始插入的数据完整写入 bug 报告和复现脚本。
2. 至少把以下信息也一并记录下来并传入复现脚本：
   - `schema_seed`
   - `query_seed`
   - `rows_per_table`
   - `use_z3`
   - `z3_timeout`

更稳妥的方案仍然是方案 1，因为即使种子一致，也可能因为生成器后续修改而导致历史 bug 无法稳定复现。

### 4.2 当前对“同名列投影”的处理是规避而不是支持


现在的处理方式是：

- 在 `compare.py` 中，如果规范化后的短列名冲突，就保留原始列名，避免覆盖
- 在 `ir_gen.py` 中，`Project` 阶段主动去掉短列名冲突的候选字段

这个修改解决了一个真实问题：以前 `o.id` 和 `u.id` 在比较器里会被都规范化成 `id`，后出现的值会把前面的值覆盖掉。

但新的问题是：

当前框架实际上开始“回避”这一类查询，而不是“正确支持”这一类查询。

这意味着不再测试这类 ORM 很容易出 bug 的场景：

- 多表 join 后投影多个同名列
- ORM 自动重命名列标签
- SQL 返回列名与 ORM label 不一致

#### 修复

长期看，应当让比较器真正支持“列身份映射”，而不是依赖简单短名规范化。

方案包括：

1. 在比较时保留 IR 的投影顺序和原始字段身份。
2. 将结果规范化为“有序元组”而不是仅靠列名字典。
3. 在比较器里显式维护：
   - IR 字段名
   - SQL 返回列名
   - ORM label
   三者之间的对应关系。

如果这样做，`o.id` 和 `u.id` 这种场景就不需要在 IR 生成阶段被删掉。

### 4.3 Z3 数据生成能力太弱

`generator/data_gen.py` 的文档写得比当前实现能力强很多。

需要实现：

- 处理 `Filter / Having`
- 为“每张表的每一行”建立约束

但当前实际实现是：

- 只对第一行建立 Z3 变量
- `Having` 直接跳过
- 多表条件的字段解析依赖模糊匹配

这会带来几个后果：

1. 很多查询虽然启用了 Z3，实际上并没有真正得到“非空结果保证”。
2. `Having` 相关查询仍然主要依赖随机数据，覆盖率不稳定。
3. 别名解析可能出错，例如多个表别名首字母相同的时候。

#### 修复

建议分阶段推进：

第一阶段：

- 先把注释和真实能力对齐，明确说明“当前只对第一行、简单数值 `Filter` 生效”。

第二阶段：

- 不再用模糊匹配别名，而是在 IR 生成或遍历阶段建立“别名 -> 表名”的显式映射。

第三阶段：

- 支持真正的多行约束
- 支持 `JOIN` 相关联动约束
- 支持 `HAVING` 约束

如果短期不打算扩展 Z3，那么至少要把它定位为“轻量启发式数据增强”，而不是“完整约束求解器”。

### 4.4 参考路径 `python_ref` 仍然是单点真值来源

当前整个系统默认：

- `python_ref` 是基准
- `sql` 和 `orm` 都拿它来对比

这在工程上是常见做法，但它带来一个理论风险：

如果 `python_ref` 本身错了，那么：

- `sql` 和 `orm` 同时与它不一致时
- 系统只能说“ref 路径可能有 bug”

也就是说，当前框架还不能稳定定位：

- 是 SQL 错
- 是 ORM 错
- 还是参考实现错

#### 修复

可以考虑以下增强方向：

1. 为 `python_ref` 增加更强的单元测试和人工验证样例。
2. 引入更多小规模手工构造的黄金 case。
3. 对某些简单 IR，额外引入第四种检查方式，例如：
   - 手写 expected result
   - 或更保守的 SQL 子查询重写交叉验证

这不一定要覆盖全部查询，但至少要覆盖高风险语义点。

### 4.5 当前测试覆盖面还偏“基础查询”，对 DBMS 特性压力不够

现在系统的主要生成范围是：

- 数值比较
- 基础聚合
- 等值 join

当前还缺少很多更容易触发数据库差异的点，例如：

- `NULL` 语义
- 字符串比较
- 类型转换
- 布尔逻辑边界
- 重名列处理
- 聚合精度
- 更多 join 结构

#### 修复

后续可以逐步加入：

1. `IS NULL / IS NOT NULL`
2. 字符串列和字符串谓词
3. 更丰富的 join 组合
4. 更复杂的布尔条件树
5. 聚合后再投影、再 having 的更复杂场景
6. 明确测试 DBMS 在 `AVG`、`COUNT(NULL)`、`NULL` 比较上的细节差异

### 4.6 测试代码组织还比较弱

当前 `tests/manual_cases.py` 还是空文件，回归测试体系还没有真正建立起来。

目前很多验证都写在各模块自己的 `__main__` 中，这适合原型调试，但不适合长期维护。

#### 修复

建议尽快把以下内容转成正式测试：

- 单表 Filter
- GroupBy + Having
- Join + Filter
- 重名投影列
- 聚合精度容差
- `NULL` 处理
- 比较器规范化逻辑
- bug 复现脚本一致性

否则每次修改比较器或生成器时，都可能不小心破坏旧行为。

### 4.7 路径导入方式不够稳健

当前多个文件里仍然使用：

- `__file__.rsplit("/db", 1)`
- `__file__.rsplit("/translators", 1)`
- `__file__.rsplit("/comparator", 1)`

这类写法依赖路径中出现 Unix 风格的 `/`，在 Windows 下并不稳健。

#### 修复

统一改成基于 `os.path` 的写法，例如：

```python
ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, ROOT)
```

这样更稳定，也更容易维护。


## 5. 建议的修复优先级

如果按“性价比最高”的顺序推进，我建议这样排：

### 第一优先级：保证 bug 可复现

优先修：

- `runner.py` 里的 bug 复现脚本
- 不要仅靠重新生成数据

因为没有可靠复现，就很难继续调试，也很难写论文时展示 bug case。

### 第二优先级：补强比较器

优先修：

- 结果比较中的列身份映射
- 重名列处理
- 聚合精度策略

因为差分测试系统最怕“oracle 不可靠”。如果比较器不稳，后面的结果统计都会受影响。

### 第三优先级：把关键问题转成回归测试

重点写测试：

- 比较器冲突列 case
- 聚合容差 case
- 多表 join 同名列 case
- repro 一致性 case

这一步会显著提升后续修改时的安全性。

### 第四优先级：扩展 Z3 或收缩 Z3 目标

二选一：

1. 继续增强 Z3，真正支持更多语义
2. 明确把 Z3 定位为“轻量辅助”，避免过度宣传其能力

当前最怕的是“代码能力 A，文档说成 B”。

### 第五优先级：扩展查询与数据覆盖面

在框架稳定后，再逐步增加：

- `NULL`
- 字符串
- 类型转换
- 更复杂布尔表达式
- 更多 DBMS 差异相关特性


## 6. 我对当前测试思路的总体评价

总体上，我认为当前 RetORM 的方向是对的，而且已经有了一个很不错的原型雏形。

它最有价值的地方在于：

- 不是把 ORM 当成黑箱乱 fuzz
- 而是先抽象出统一语义，再做多路径等价验证

这个思路比单纯堆手工 case 更有研究价值，也更容易扩展。

但目前的主要问题是：

- 框架主干已经搭好了
- 真正最难的部分，即“可靠 oracle + 稳定 repro + 困难语义覆盖”，还没有完全打牢

换句话说，当前代码更像是：

> 一个已经证明“这条路线可行”的研究原型  
> 但距离“高可信、可发表、可扩展的测试系统”还差几个关键工程点


## 7. 后续建议路线图

如果你接下来继续完善 RetORM，我建议按下面路线走：

### 阶段一：把框架做稳

- 修复 repro 不一致问题
- 统一路径导入方式
- 为比较器补正式测试
- 为参考执行器补黄金 case

### 阶段二：把 oracle 做强

- 正确支持重名列
- 正确支持更复杂标签映射
- 区分“语义等价”与“格式等价”
- 细化浮点与聚合容差策略

### 阶段三：把覆盖做深

- `NULL`
- 字符串
- 类型转换
- 更多 join / group / having 组合
- 更复杂约束生成

### 阶段四：把研究价值做出来

- 统计 bug 类型
- 记录不同路径之间的失配模式
- 分析“为什么 ORM/SQL/参考实现不一致”
- 做 DBMS-specific case 分类


## 8. 总结

当前 RetORM 已经实现了一个比较完整的差分测试原型：

- 有统一 IR
- 有三条执行路径
- 有数据生成
- 有结果比较
- 有 bug 记录与复现导出

这是当前代码最值得肯定的地方。

同时，它也还存在几类关键问题：

- bug 复现脚本不稳定
- 比较器对重名列的支持还不彻底
- Z3 能力与注释描述不一致
- 参考路径仍然是单点真值来源
- 测试覆盖面还偏基础

所以我对它的判断是：

> 当前版本已经是“能说明方法成立”的原型  
> 下一步最重要的不是继续加功能，而是先把 oracle、repro 和回归测试体系打牢

如果这三件事先做好，后面无论是继续扩展查询特性，还是把它整理成论文系统，都会顺很多。
